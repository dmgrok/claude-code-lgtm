## Token Efficiency Guidelines

- Prefer targeted file reads with offset/limit over reading entire files
- Use grep/find to locate relevant code before reading full files
- Keep explanations concise — bullet points over prose for technical output
- For multi-file changes, plan first, execute in batch
- Avoid re-reading files just written (Write/Edit confirms success)
- When spawning subagents, give them narrow single-purpose tasks
